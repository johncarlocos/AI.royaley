# ROYALEY - GPU Dependencies (Fixed)
# Version: 2.0.1
# For Enterprise Multi-GPU (4x RTX A6000)
#
# NOTE: This file should be installed AFTER requirements.txt
# Do NOT use -r requirements.txt here (causes duplicate installs)

# ============================================================================
# PyTorch with CUDA 12.1
# ============================================================================
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.1.2+cu121
torchvision==0.16.2+cu121
torchaudio==2.1.2+cu121

# ============================================================================
# CUDA-accelerated libraries (pip-compatible only)
# ============================================================================
cupy-cuda12x>=13.0.0

# ============================================================================
# TensorFlow CUDA Runtime Libraries
# TF 2.15 needs these to use GPU. They provide libcudart, libcublas,
# libcudnn etc. as pip-installed .so files inside the venv.
# Dockerfile sets LD_LIBRARY_PATH to point TF to these locations.
# ============================================================================
nvidia-cublas-cu12
nvidia-cuda-cupti-cu12
nvidia-cuda-nvrtc-cu12
nvidia-cuda-runtime-cu12
nvidia-cudnn-cu12==8.9.*
nvidia-cufft-cu12
nvidia-curand-cu12
nvidia-cusolver-cu12
nvidia-cusparse-cu12
nvidia-nccl-cu12
nvidia-nvjitlink-cu12

# ============================================================================
# GPU Monitoring
# ============================================================================
nvidia-ml-py>=12.535.133
pynvml>=11.5.0
gpustat>=1.1.1

# ============================================================================
# ONNX Runtime with GPU
# ============================================================================
onnxruntime-gpu>=1.16.0

# ============================================================================
# RAPIDS (cuML, cuDF) - CANNOT be installed via pip
# ============================================================================
# RAPIDS libraries require conda installation:
#   conda install -c rapidsai -c conda-forge -c nvidia \
#       cuml=24.02 cudf=24.02 python=3.11 cuda-version=12.0
#
# Or use NVIDIA NGC container:
#   docker pull nvcr.io/nvidia/rapidsai/rapidsai:24.02-cuda12.0-runtime-ubuntu22.04-py3.11

# ============================================================================
# TensorRT - Requires special NVIDIA setup (commented out)
# ============================================================================
# TensorRT needs to be installed from NVIDIA directly:
#   pip install tensorrt --extra-index-url https://pypi.nvidia.com

# ============================================================================
# Horovod - Complex build, optional (commented out)
# ============================================================================
# Horovod requires MPI and special build flags:
#   HOROVOD_WITH_PYTORCH=1 HOROVOD_WITH_TENSORFLOW=1 pip install horovod

# ============================================================================
# Ray for Distributed Training (optional, commented out)
# ============================================================================
# ray[default]>=2.9.0